

import UIKit

enum MessageInputType: String {
    case talk = "talkView"
    case camera = "cameraInput"
    case message = "messageInput"
    case reply = "messageReply"
    case sticker = "messageSticker"
}
